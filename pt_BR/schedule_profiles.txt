sch_profiles = Perfis de Agendamentos
menu.schedules_profiles = {:sch_profiles:}
sch_profiles.validate.wrong_expression = %s não é uma expressão cron válida
sch_profiles.desc.title = Descrição para este perfil de agendamento.
sch_profiles.profile = Modelo
sch_profiles.profile.title = Assistente de modelo rápido
sch_profiles.schedule = Agenda
sch_profiles.minute = Minuto
sch_profiles.hour = Hora
sch_profiles.day = Dia do mês
sch_profiles.month = Mês
sch_profiles.weekday = Dia da semana
sch_profiles.task.title = :digite para executar a tarefa
sch_profiles.select_tpl = -- Selecione um modelo --
sch_profiles.tpl.once_per_minute = Uma vez por minuto (* * * * *)
sch_profiles.tpl.once_each_five = Uma vez a cada cinco minutos (*/5 * * * *)
sch_profiles.tpl.twice_per_hour = Duas vezes por hora (0,30 * * * *)
sch_profiles.tpl.once_per_hour = Uma vez por hora (0 * * * *)
sch_profiles.tpl.twice_per_day = Duas vezes por dia (0 0,12 * * *)
sch_profiles.tpl.once_per_day = Uma vez por dia (0 0 * * *)
sch_profiles.tpl.once_per_week = Uma vez por semana (0 0 * * 0)
sch_profiles.tpl.1st_15th = Nos dias 1 e 15 de cada mês (0 0 1,15 * *)
sch_profiles.tpl.once_per_month = Uma vez por mês (0 0 1 * *)
sch_profiles.tpl.once_per_year = Uma vez por ano (0 0 1 1 *)
sch_profiles.quick_templates = -- Modelos rápidos --
sch_profiles.qtpl.each_minute = Cada minuto (*)
sch_profiles.qtpl.each_two_minutes = Cada dois minutos (*/2)
sch_profiles.qtpl.each_five_minutes = A cada cinco minutos (*/5)
sch_profiles.qtpl.each_ten_minutes = A cada dez minutos (*/10)
sch_profiles.qtpl.each_fifteen_minutes = Cada quinze minutos (*/15)
sch_profiles.qtpl.each_thirty_minutes = A cada trinta minutos (0,30)
sch_profiles.qtpl.every_hour = A cada hora (*)
sch_profiles.qtpl.each_two_hours = Cada duas horas (*/2)
sch_profiles.qtpl.each_three_hours = Cada três horas (*/3)
sch_profiles.qtpl.each_four_hours = Cada quatro horas (*/4)
sch_profiles.qtpl.each_six_hours = Cada seis horas (*/6)
sch_profiles.qtpl.each_twelve_hours = Cada doze horas (0,12)
sch_profiles.qtpl.every_day = Todos os dias (*)
sch_profiles.qtpl.every_two_days = A cada dois dias (*/2)
sch_profiles.qtpl.1st_15th = Nos dias 1 e 15 do mês (1,15)
sch_profiles.qtpl.every_month = Todo mês (*)
sch_profiles.qtpl.every_two_months = A cada dois meses (*/2)
sch_profiles.qtpl.every_three_months = A cada três meses (*/3)
sch_profiles.qtpl.every_six_months = A cada seis meses (*/6)
sch_profiles.qtpl.every_weekday = Todos os dias da semana (1-5)
sch_profiles.qtpl.every_weekend = Todo fim de semana (6,0)
sch_profiles.qtpl.specific_days = Toda segunda, quarta e sexta (1,3,5)
sch_profiles.qtpl.specific_days_2 = Toda terça e quinta (2,4)
sch_profiles.tpl.on_sunday = Às 00:00 de domingo (0 0 * * 0)
sch_profiles.tpl.on_saturday = Às 00:00 de sábado (0 0 * * 6)
sch_profiles.select_sch = -- Selecione um agendamento --